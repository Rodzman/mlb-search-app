import BreadcrumbPresentation from '../presentation';
import type { Props } from '../types';

const BreadcrumbContainer = (props: Props) => {
  return <BreadcrumbPresentation {...props} />;
};

export default BreadcrumbContainer;
