import ContentPresentation from '../presentation';
import type { Props } from '../types';

const ContentContainer = (props: Props) => {
  return <ContentPresentation {...props} />;
};

export default ContentContainer;
