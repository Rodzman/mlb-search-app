import ItemsListPresentation from '../presentation';
import type { Props } from '../types';

const ItemsListContainer = (props: Props) => {
  return <ItemsListPresentation {...props} />;
};

export default ItemsListContainer;
