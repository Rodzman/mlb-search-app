import MainPresentation from '../presentation';
import type { Props } from '../types';

const MainContainer = (props: Props) => {
  return <MainPresentation {...props} />;
};

export default MainContainer;
