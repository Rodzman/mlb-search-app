import ShowComponentPresentation from '../presentation';
import type { Props } from '../types';

const ShowComponentContainer = (props: Props) => {
  return <ShowComponentPresentation {...props} />;
};

export default ShowComponentContainer;
